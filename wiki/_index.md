# Wiki

Distilled, reusable knowledge — patterns, checklists, gotchas. **Only harvested from shipped projects.** Never written directly.

Every wiki note carries a `source:` line pointing back to the project it was harvested from. If you're about to write a wiki note without a shipped source, stop — it belongs in `projects/` or `inbox/`, not here.

## Contents
_(empty — populated by `/harvest` from shipped projects)_

## First harvest candidates
Real shipped work queued for harvesting:

- Cloudflare Worker + bearer auth + secrets pattern (source: API proxy)
- Stripe + Railway deployment checklist (source: agent-factoria)
- Per-platform voice contracts + publish caps pattern (source: lape-ventures / Distributor)
- Spec-to-ship pipeline w/ PreToolUse guard hook + test gate (source: factory-demo)
- KDP print-on-demand build checklist: bleed dims, interior workflow, metadata/pricing (source: backyard-teachers-book)
- A/B positioning simulation method (source: FVF MiroFish-style tests)
